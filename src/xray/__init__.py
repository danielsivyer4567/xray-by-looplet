"""X-Ray by Looplet — sees through plans. PDF in, quantities out.

Headless construction-plan takeoff engine. An LLM never produces a quantity:
quantities come only from deterministic grammar + geometry + rules.
"""

ENGINE_NAME = "xray-by-looplet"
__version__ = "0.1.0"
