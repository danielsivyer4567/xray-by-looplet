---
name: sitemark-takeoff-engine
description: X-Ray by Looplet (né SiteMark takeoff engine; Bluebeam-competitor core) — headless plan-takeoff engine at C:\repos\xray-by-looplet; Bluebeam internals dossier verified
metadata: 
  node_type: memory
  type: project
  originSessionId: 743d0188-106e-4a15-bcfe-09f47f4119f2
  modified: 2026-07-23T04:45:00.318Z
---

**2026-07-21 PRODUCT = "X-Ray by Looplet"** (Daniel's final branding; house = Looplet, instrument = X-Ray; was SiteMark takeoff engine). Repo: **C:\repos\xray-by-looplet** (scaffolded as C:\repos\sitemark-takeoff-engine, rename sweep after first build verifies: package `xray`, CLI `python -m xray run <pdf>`, engine JSON name "xray-by-looplet", /T "X-Ray by Looplet"). Headless-core architecture Daniel-approved: engine emits takeoff.json + Revu-compatible marked.pdf; NO UI — presentation delegated to Looplet CRM / LLM formatter (LLM never produces quantities); future Bluebeam-competitor viewer = "viewer on top of proven engine" (the SiteMark Pro screenshot was a concept mock; no pre-existing repo ever found).

**Takeoff engine designed + empirically proven** (7-stage pipeline: extract→OCR→grammar→scale-vote→associate→quantify→reconcile; LLM never produces quantities):
- Fixture 1: `C:\Users\danie\OneDrive\Desktop\TRADE EASE\aaaa looplet documents\PAGES\payment modal options\Building plans and documentation (1).pdf` (Design21 warehouse set, 13 vector sheets; "(1) (1).pdf" is byte-identical dupe). Finding: CAD exports **glyph-split dimension text** (29995 etc. present only in squished text) → Module 1 = glyph-run reassembler; Module 2 = dimension-line associator (y-banding provably insufficient).
- Fixture 2: `C:\Users\danie\AppData\Local\Temp\MicrosoftEdgeDownloads\7c8b1d4d-8656-477d-9145-7cde0253a096\MANNERS - SHED PLANS.pdf` (A-Line/North VIC Sheds garage, Skia/Chromium print, whole words). ALL 3 chains sum to 16000 exact; spec token `16Lx9Wx4.2H|10°|4bays` parsed; trig check tan(10°)×4500=793 matches drawn gable; 5 PORTAL RAFTER labels. False-positive chains from phone/postcode/copyright → needs title-block masking.
- Scripts (session scratchpad 743d0188…/scratchpad): dissect_revu.py, probe_layers.py, extract_entities.py — copy into repo when found.

**Bluebeam internals dossier** (2 deep-research runs, 21+25 claims verified 3-0; full outputs in session tasks war3xciko.output + wlvrmkrd3.output):
- Markup keys: /NM=UUID, /Subj, /Label (custom), /T, /OC; Spaces = page-level /BSISpaces tree (Title/Path/Kids); custom columns = per-annot /BSIColumnData + doc-level def (Revu exports as XML via ColumnsExport); measurements = /MeasurementTypes bitflags (128 Count/129 Area/130 Length/132 Volume/384 Dia/1152 Angle — observed subset, medium conf) + /DepthUnit + /BBMeasure + standard /Measure + /IT intents. **pymkup (github psolin/pymkup) = Rosetta stone.**
- Sidecars all XML: .btx (tool set), .bax (markup interchange), .bpx (profile, can bundle tool sets); Markup Summary CSV has ID/Parent columns = grouping schema (CSV only). Script API = 31 commands, no OCR/measure/scale.
- PATENT LANDMINES (counsel before shipping): Dynamic Fill US10,452,751/11,087,069 — only ANIMATED time-segmented fill claimed (instant flood-fill outside literal claims; pending AU continuation AU2025271241A matters — Daniel is AU); auto-legend family US10,534,859/11,182,543/12,236,184 — avoid tool-set-linked LIVE-updating legend; Visual Search US9,846,707 (color/size pre-filter); Studio-sync family (translate-to-JSON neutral object) live to ~2039. 27-patent marking list at bluebeam.com/legal/intellectual-property. REFUTED: palette-patent + compare-patent expiry stories — re-pull before relying.
- Open: where Revu stores page scale/viewports in PDF; BTX internal schema; Revu OCR engine; Studio REST API — all need a real Revu-authored PDF/.btx (NONE exist on Daniel's machine; profile-wide scan came up empty).

**2026-07-23 — full session; canonical handoff now at `xray-by-looplet/HANDOFF.md` (3aaad6c, supersedes old CONTEXT). See also [[xray-visualization-roadmap]].**

**2026-07-23 — branches landed + PDFium determinism landmine.** Pushed `feat/assemblies` (Layer A wall recipe) and `feat/desktop-electron` (Electron shell + `parity/` gate) to origin; both unmerged, PRs not opened. Frozen PyInstaller sidecar BUILT and verified **3/3 byte-identical** to the Python oracle, so the desktop spawn-per-takeoff architecture is proven end to end (`desktop\engine\bin\xray-engine.exe`, ~80 MB).

- **PDFium is NOT the cause — earlier claim RETRACTED (c8a145a).** Tested directly: engine output is **byte-identical under PDFium 149.0.7802.0 (pypdfium2 5.7.1) and 152.0.7947.0 (5.12.1)** on all 3 fixtures. Also unchanged across engine commits 2748f4f/cfe0771/b35ae3e/33fdfd2/ee8b25e, and `fixtures/` is unmodified since 6533f1a. So **the WASM engine does NOT need to match a specific PDFium build** — that §5 constraint is unsupported. Pin `pypdfium2` exactly anyway (hygiene; `>=4` today).
- **warehouse 0-quantities RESOLVED 2026-07-23 (73fc6b8):** correct, not a bug — only `shed` + `electrical` packs exist and neither detects it. But it was SILENT, and `run_packs` had `except Exception: pass` so a *crashed* pack looked identical to *no pack*. Now: pack failures → flagged `pack-error` checks (naming pack + exception); empty takeoff → `pack-coverage` check listing measurable trades. Both reach `review[]`. New schema check kinds: `pack-coverage`, `pack-error`. Only the warehouse reference moved (shed/electrical byte-identical) — sidecar rebuilt + re-gated 3/3. **Adding a new trade = a new pack module** (packs.register).
- CRM `/health` probe now requires `engine === 'xray-by-looplet'` — `VITE_XRAY_API` default :8000 is occupied on Daniel's machine, and trusting any 200 showed a false "ready".
- **OPEN: 2 legacy digests unreproducible.** shed matches its §5 pin exactly (`6b3a766ca4aa5348`); electrical-schedule and warehouse-design21 never do under any combination tested — likely produced from different fixture files or engine state. This repo's own `parity/reference/` is the oracle until resolved. Also: **warehouse-design21 yields 0 quantities and no test asserts them** — possible silent gap.
- Version map for future pinning: pypdfium2 5.7.1→PDFium 149.0.7802.0, 5.8.0→149.0.7825.0, 5.9.0→150.0.7869.0, 5.10.1→151.0.7891.0, 5.11.0→151.0.7920.0, 5.12.1→152.0.7947.0.
- Canonical form (confirmed against the pinned shed hash): strip `document.path`, then `json.dumps(sort_keys=True, separators=(',',':'), ensure_ascii=False)`, sha256 of UTF-8 bytes.
- **The cloud session's `pricing/` package is LOST** — `suppliers.json` / `registry.py` / `test_registry.py` only ever existed in that session's `/home/claude/out/` and are NOT on this machine. `parity/` was rebuilt from scratch here; **pricing must be re-supplied by Daniel** (do NOT reinvent 60 suppliers — that would be fabricated business data). Same for the .xlsx order-sheet/registry deliverables and `XRAY-LOOPLET-INTEGRATION.md`.
- **Oxworks IMPORTED 2026-07-23 (37a4b1a).** ⚠️ The "OneDrive can't be granted" note was WRONG — it applied to chat attachment, not the filesystem. `C:\Users\danie\OneDrive\Documents\Oxworks+Full+Pricelist+-+Current+East+Coast.pdf` (94 MB, 274 pp, effective 26 Sep 2025) reads fine via Python/PowerShell. **Always test file access before believing a stated constraint.** Importer = `pricing/oxworks.py` (`python -m pricing.oxworks <pdf> --out <base>` → .json + .csv; `--report-only` for the quality report). Result: 3,522 rows / 1,111 SKUs / 0 unparsed / 100 POA. **Generated catalogue is gitignored — importer versioned, prices NOT** (commercially sensitive). Ignore the stale Feb-2023 copy on the Desktop.
- **SKU mapper BUILT 2026-07-23 (5bb3bf0):** `pricing/mapping.py` — **proposes, never decides**. Confirmed mappings remembered per supplier in `pricing/out/<supplier>-mappings.json` (gitignored), keyed on normalised item **+ unit**. Units are a hard gate (lm can't fulfil m2). Deterministic token+dimension scoring, no LLM. WHY no auto-accept: "65x16 Radiator Panel 1800MM HIGH" matches **3 rows at 1.00 with prices $480/$393/$348** (pp136/137/138) — top-hit auto-accept = 38% overcharge. Shed takeoff maps **0/13** vs Oxworks — correct, Oxworks is fencing not shed steel.
- ⚠️ **The harness reported this PDF as "967 pages" — WRONG.** pypdfium2 + pikepdf(QPDF) + the PDF's own `/Count` all say **274**, and printed page numbers run 2→271 with a constant +3 offset. Desktop and Documents copies are byte-identical (sha256 0A2819DD…). Don't re-import believing pages were missed.
- Oxworks parser gotchas (apply to the next supplier): bind to row SHAPE (code left, money right), not header position — ~300 tables with varying columns. Units are written 5+ ways ("[Per Metre]", "Per Metre Standard", "Per Metre x 1200H", "Per Mtr", "Per Lineal Metre") — matching only the bracketed form mistyped ~1,900 lm rows as `ea`. 40 rows have NO SKU column (leftmost token is literally "PRICE"). POA must stay null, never 0.
- **STANDALONE VERIFIED 2026-07-23** (Daniel's requirement: X-Ray must run isolated, NOT depend on the CRM). `xray-by-looplet/desktop` launches, renders from `file://`, and ran a real takeoff end-to-end — renderer → preload → IPC → frozen sidecar → 13 quantities, no CRM/Python/server. Same numbers across 4 surfaces (Python oracle, frozen exe CLI, FastAPI, Electron IPC). Launch recipe: **must `unset ELECTRON_RUN_AS_NODE`** (Claude Code sets it; breaks Electron — CRM's dev-launch.cjs deletes it too). Drive it headlessly via `--remote-debugging-port` + CDP `Runtime.evaluate` on `window.xray.runTakeoff(path)` — bypasses the native file dialog. **CDP `Page.captureScreenshot` HANGS if the window is minimized** — restore it first.
- **Host kit DONE (2026-07-23):** spawn logic de-duplicated into **`xray-by-looplet/host/xray-host.js`** (`@looplet/xray-host`, zero-dep CJS, 10 node:test tests). `desktop/` consumes it as a `file:../host` dep (a `../host` require would resolve in dev then vanish from the electron-builder installer). CRM **vendors it verbatim** to `electron/main/vendor/` (separate remotes ⇒ no package dep possible) with provenance json + `scripts/sync-xray-host.mjs` + a drift test **proven to fire on tamper**; needed `vitest.config.ts` include (outside src/) + `.gitattributes eol=lf` (drift test hashes bytes; `* text=auto` would break it on CRLF machines) + `electron.vite.config.ts commonjsOptions.include` (rollup can't read `module.exports={}` named exports outside node_modules — **main bundle failed outright until fixed; caught by running the build, not by reasoning**).
- **`feat/fixes` (CRM base) CANNOT BUILD.** Base commit c708d0caec imports `./TemplatePreviewSheet` (and `panels/inspector/setup-checklist`) which are NOT tracked there — they exist only as uncommitted files in the `Looplet-automations-builder-port` worktree. Renderer bundle fails; electron main+preload build fine. Whoever owns that branch must commit those files.
- Env gotchas: `python` on PATH is the **hermes-agent venv** (`C:\Users\danie\AppData\Local\hermes\hermes-agent\venv`) — that's where the repo's deps and now pyinstaller live; `python -m xray` needs `PYTHONPATH=src`. The **looplet-crm `check-gate` hook intercepts commits in OTHER repos** (write `.claude/scratch/check-passed` in a separate call). Stale `.git\HEAD.lock` + `objects\maintenance.lock` with no git process running recur here — move them aside to recover.

**2026-07-23 — /xray embed: shell question settled + bridge built.** New worktree `C:\repos\looplet webb app\looplet crm\Looplet-xray-embed` on **`feat/xray-embed`** (LOCAL, not pushed), branched off `feat/fixes` HEAD c708d0caec because /xray only exists in that lineage.

- **The CRM shell IS Electron** (electron 40 + electron-builder + `electron/main` + `electron/preload`, no src-tauri). So the engine bridge is `electron/main/xray-ipc.ts` in **Looplet-automations-builder-port**, beside the UI, following the existing `db-ipc`/`everything-ipc` `setupXIPC()` pattern. **looplet-producer is NOT involved** — the old integration plan was wrong. Desktop now spawns the frozen sidecar per takeoff (channels `xray:status|pick-pdf|run`); FastAPI is the browser-only path.
- **`isExtensionEnabled()` does not exist.** The "Extension Store" is a static `extensions: Extension[]` array in `src/features/extensions/ExtensionsPage.tsx` — a catalogue UI, no gating. Routes are open. X-Ray added there as a construction tile.
- `/xray` route + page were ALREADY committed on feat/fixes before this session — only the web-only engine-client rewrite was uncommitted.
- **Base branch is not clean:** `feat/fixes` carries 16 pre-existing tsc errors in `src/features/automations` + `components/layout` (one references `setup-checklist`, which exists only as an uncommitted file in the sibling worktree). Don't attribute them to xray work.
- **Shared-worktree hazard, live:** `Looplet-automations-builder-port` had ~32 dirty files from an active automations ledger. The 2 xray files were COPIED out, not moved — those copies are still dirty there and should be discarded once feat/xray-embed is accepted.
- Port collisions on this machine: **:8000 is occupied by an unrelated uv-python service** (returns 404 on /health) and **:5173 by another Vite** — so `VITE_XRAY_API`'s default `127.0.0.1:8000` points at the WRONG server. Test with `--port 8001` + `VITE_XRAY_API`. `npm run dev:web` runs the renderer without Electron; the full app redirects to `/` unauthenticated, so browser-driving `/xray` needs a login.

Market wedges (run 1): Revu 20 EOL Dec 31 2026 = timed switching wave; Dynamic Fill/Quantity Link/Batch gated behind $440 Complete tier — ship ungated; Bluebeam Cloud can't round-trip its own measurements/custom tools; ~A$737/yr AU pricing unverified.
